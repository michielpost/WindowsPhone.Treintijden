using System.Collections.Generic;

namespace ActueelNS.Services.Models
{
    //public class StationsByName : List<StationInGroup>
    //{
    //    private static readonly string Groups = "#abcdefghijklmnopqrstuvwxyz";

    //    private Dictionary<int, Station> _personLookup = new Dictionary<int, Station>();

    //    public StationsByName(List<Station> stationsList)
    //    {
    //        stationsList.Sort(Station.CompareByFirstName);

    //        Dictionary<string, StationInGroup> groups = new Dictionary<string, StationInGroup>();

    //        foreach (char c in Groups)
    //        {
    //            StationInGroup group = new StationInGroup(c.ToString());
    //            this.Add(group);
    //            groups[c.ToString()] = group;
    //        }

    //        foreach (Station person in stationsList)
    //        {
    //            groups[Station.GetFirstNameKey(person)].Add(person);
    //        }
    //    }
    //}
}